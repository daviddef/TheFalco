{
  "name": "falco-archive",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 4322",
    "build": "astro build && npm run check && npm run sitemap",
    "preview": "astro preview --port 4322",
    "check": "python3 ../tools/check_release.py",
    "build:nocheck": "astro build",
    "sitemap": "python3 ../tools/sitemap.py"
  },
  "dependencies": {
    "@daviddef/archive-kit": "github:daviddef/TheArchiveKit",
    "astro": "^5.18.2"
  }
}
